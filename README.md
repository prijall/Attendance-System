# Attendance-System
Attendance system using CV

## Setup Instructions (with `setup.sh`)

1. Clone the repository:
   ```bash
   git clone https://github.com/prijall/Attendance-System.git
   cd repository

2. For installing dependencies:
```bash
  pip install -r requirements.txt
